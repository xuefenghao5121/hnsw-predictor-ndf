# NDF close plan: `cgroup-v1-support`

> Generated by `spec/meta/tools/ndf_close.py` at 2026-08-06T13:32:45Z
> mode: **promote**
> Principle: **add nodes/edges onto the Trunk NDF graph** (`spec/00–50` + `spec/meta`); do **not** lift `poc/*/ndf` into a second SoT.

## 1. Inventory

| field | value |
|-------|-------|
| topic_id | `cgroup-v1-support` |
| TOPIC status | ready-for-promote |
| binder | `poc/cgroup-v1-support/ndf/TOPIC.md` |
| baseline_protocol | [[CON-SLA-014]] + [[CON-SLA-016]] + [[CON-SLA-017]] + [[CON-SLA-018]] |
| depends_on_topics | (none) |
| draft_ids (TOPIC) | `BEH-032`, `API-016` |
| trunk topic= meta | — |

### Proposals

| Role | Path | Status |
|------|------|--------|
| root | `../../spec/open/proposal-cgroup-v1-support.md` | draft (已验证) |

### Clause locations in Trunk

| ID | status | level | file:line | meta edges |
|----|--------|-------|-----------|------------|
| `BEH-032` | **MISSING** | — | — | — |
| `API-016` | **MISSING** | — | — | — |
| `DEC-079` | **MISSING** | — | — | — |

## 2. Graph addition plan (onto Trunk SoT)

Only confirmed dispositions below should be applied to `spec/00–50`. POC binder remains non-SoT.

| ID | disposition | suggested Trunk action |
|----|-------------|------------------------|
| `BEH-032` | `promote-to-stable` | Set `status=stable` (keep `topic=cgroup-v1-support` until archive or clear per promote proposal); ensure level/must appropriate; file `TBD` |
| `API-016` | `promote-to-stable` | Set `status=stable` (keep `topic=cgroup-v1-support` until archive or clear per promote proposal); ensure level/must appropriate; file `TBD` |
| `DEC-079` | `promote-to-stable` | Set `status=stable` (keep `topic=cgroup-v1-support` until archive or clear per promote proposal); ensure level/must appropriate; file `TBD` |

### Add nodes

- upgrade `BEH-032` draft→stable in `TBD`
- upgrade `API-016` draft→stable in `TBD`
- upgrade `DEC-079` draft→stable in `TBD`

### Add / update edges

- Review existing meta edges; attach new clauses to Trunk parents only.

### Do not add

- POC `evidence/` QPS/Recall numbers as `status=stable` must SLA ([[CON-POC-001]])
- Entire `poc/<topic>/ndf/` tree as a second SoT
- Unconfirmed amend proposals without draft→stable ID list

### Suggested free IDs (if creating new clauses)

- `BEH-029`
- `API-014`
- `CON-SLA-019`
- `VER-043`
- `DEC-077`

## 3. Prose provenance (POC 溯源 — required)

Any prose copied into Trunk (rationale, DEC body, VER notes, promote write-up) **MUST** retain source lines:

```text
> source: poc/cgroup-v1-support/ndf/TOPIC.md ; proposals/<file> ; evidence/ ; COMMITS.md @ —
> track: promote ; Topic: cgroup-v1-support
```

Per-artifact templates:

- Proposal `../../spec/open/proposal-cgroup-v1-support.md`:
  ```text
  > source: ../../spec/open/proposal-cgroup-v1-support.md (binder role=root, status=draft (已验证))
  > track: promote ; Topic: cgroup-v1-support
  ```
- Evidence dir `poc/cgroup-v1-support/ndf/evidence`: cite round + date; do not paste as must SLA.
- TOPIC narrative (`Active hypothesis` / results): `poc/cgroup-v1-support/ndf/TOPIC.md`

**Forbidden:** pasting POC prose into `spec/00–50` without `source:` lines.

## 4. Archive / binder checklist (not executed by this tool)

- [ ] Set `TOPIC.md` status → `promoted`
- [ ] Record `src_commit` + `spec_commit` in `COMMITS.md`
- [ ] Move binder → `spec/archive/2026-08/poc-cgroup-v1-support/` (or summary pointer)
- [ ] Commit trailers: `Topic:`, `Proposals:`, `Clauses:`, `Promotes: cgroup-v1-support`
- [ ] `spec/open/` stubs for Implemented proposals if still pointing at live paths

## 4b. Semantic core decision (MAY deliver; MUST decide; [[META-004]] / [[BEH-019]])

Not executed by this tool. Missing MODEL is **not** a close/graphcheck failure.

- [x] Decide: distill L3 semantic core for promoted behavior?
  - [x] **No** — reason: 测试基础设施 (shell 脚本层), 非核心搜索算法行为。
    BEH-032 的行为简单 (检测 cgroup 版本 → 选接口 → 测后验证),
    无复杂状态机/时序/invariants 需要 oracle 描述。
    L1 (BEH-032) + DEC-079 已完整描述。
    对比 MODEL-WILLNEED-001 (readahead 的 enable/timing/ops/invariants),
    cgroup 兼容层不需要这种抽象。
  - [ ] Defer
  - [ ] Yes

## 4c. Baseline invalidation (Trunk moved; [[BEH-025]] / [[BEH-019]])

Not executed by this tool. Checklist for Agent/human after merge.

- [ ] Note new Trunk `src` SHA after this close (topic=`cgroup-v1-support`, mode=`promote`)
- [ ] List exploring topics: `python3 spec/meta/tools/ndf_index.py poc-topics`
- [ ] Set `baseline_status=stale` on affected exploring topics (**including this topic if still exploring** after partial)
- [ ] Disjoint-surface siblings MAY mark N/A + reason (still SHOULD refresh `baseline_trunk_sha` when convenient)
- [ ] Do NOT treat pre-promote R0 tables as current-Trunk baseline
- [ ] Before next round on a stale topic: re-measure R0 @ current Trunk

## 4d. Surface overlap / conflict check ([[BEH-025]] / [[BEH-018]] §9)

- [ ] Read this topic `explore_surface` from TOPIC.md
- [ ] List exploring topics whose `explore_surface` intersects (from `poc-topics`)
- [ ] For each overlap: confirm `depends_on_topics` / `conflicts_with_topics` OR resolve before claiming additive gains
- [ ] Overlapping exploring siblings: `baseline_status=stale` AND `next_gate` includes conflict re-check vs promoted slice
- [ ] MUST NOT promote two overlapping topics in the same close
- [ ] Cross-topic ΔQPS: re-measure on same `baseline_trunk_sha` + protocol (gains not default-additive)

## 5. Post-merge checks (MUST run after Trunk edits)

After applying graph additions to Trunk SoT:

```bash
python3 spec/meta/tools/ndf_index.py index
python3 spec/meta/tools/ndf_graphcheck.py --report spec/open/graphcheck-after-close-cgroup-v1-support.md
```

**Pass criteria for this close:**

- `ndf_graphcheck` reports **no new hard errors** on touched IDs: `BEH-032`, `API-016`, `DEC-079`
- Pre-existing warehouse cycles (e.g. meta `CHR-008`↔`BEH-018`) may remain; document them as pre-existing — but **this close must not** add `stable/must`→`draft` refine/depends edges.
- Re-run `python3 spec/meta/tools/ndf_index.py poc-topics` to confirm binder status.

---

_End of plan. Mode=promote. No SoT files were modified by `spec/meta/tools/ndf_close.py`._
