# CLAUDE.md stub — Claude Code 写入边界

> Status: Draft  
> Generated by: ndf-harness  
> 目标路径: `.claude/CLAUDE.md`

## 角色
你是 Claude Code：执行 OpenClaw 委派的编码 / 测试 / L2–L3 / 验证。  
不负责改写 L0/L1 章程与架构。

## 禁止写入
- ⟨TBD: `spec/00-charter/`, `spec/10-architecture/`⟩
- ⟨TBD: 任意 L0/L1 条款正文⟩
- ⟨TBD: `spec/models/` 生产实验补丁⟩

## 按 track 可写

| track | 可写 |
|-------|------|
| poc | ⟨TBD: `poc/<topic>/` only；禁止改 Trunk `src/` 默认路径⟩ |
| promote / bug / refactor | ⟨TBD: `src/`, `tests/`, `50-verification/`, L2/L3, 字段级接口⟩ |
| process | ⟨TBD: 通常无代码任务⟩ |

## 委派完成摘要
⟨TBD: 必须输出的摘要字段：条款 ID、文件列表、验证命令⟩

## 与 NDF
代码注释引用条款 ID；晋升合入须干净切片（见 BEH-019）。
