# QUICKSTART

1. **Norms** — copy `norms/meta/` → `spec/meta/`；`ndf.yaml.stub` → `spec/ndf.yaml`；create empty product dirs（见 `norms/product-tree/README.md`）.  
2. **AGENTS** — copy `workflow/AGENTS.md` → repo root；fill ⟨TBD⟩ after human confirm.  
3. **Tools** — follow `governance/tools/VENDOR.md`； place scripts in `spec/meta/tools/`.  
4. **Adapter（optional）** — pick `adapters/generic|openclaw|claude-code|opencode|cursor`.  
5. **Baseline** — run commands in `governance/docs/GOVERN.md`； keep reports under `tmp/`.

Skill modes: read `skill/SKILL.md`.
