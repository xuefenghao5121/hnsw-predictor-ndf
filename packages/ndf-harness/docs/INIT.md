# INIT checklist

See also `skill/MODES.md` → init / adopt.

## Greenfield

1. Profile = `dual-track` unless user chooses otherwise.  
2. Draft install（Status: Draft）of norms + AGENTS + governance docs.  
3. Wait for human confirm.  
4. Fill ⟨TBD⟩； obtain tools； optional adapter.  
5. `ndf_index` + `ndf_graphcheck` baseline.

## Brownfield

1. Scan for existing `spec/`, `AGENTS.md`, misplaced tools.  
2. Gap report only.  
3. Never silent-overwrite finalized AGENTS or meta clauses.
