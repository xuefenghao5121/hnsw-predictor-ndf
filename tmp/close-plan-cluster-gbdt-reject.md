# NDF close plan: `cluster-gbdt`

> Generated by `spec/meta/tools/ndf_close.py` at 2026-08-15T11:55:52Z
> mode: **reject**
> Principle: **add nodes/edges onto the Trunk NDF graph** (`spec/00–50` + `spec/meta`); do **not** lift `poc/*/ndf` into a second SoT.

## 1. Inventory

| field | value |
|-------|-------|
| topic_id | `cluster-gbdt` |
| TOPIC status | exploring |
| binder | `poc/cluster-gbdt/ndf/TOPIC.md` |
| baseline_protocol | CON-SLA-020 sustained |
| depends_on_topics | vecblock-cluster-reorder (promoted) |
| draft_ids (TOPIC) | — |
| trunk topic= meta | — |

### Proposals

_(none parsed)_

### Clause locations in Trunk

| ID | status | level | file:line | meta edges |
|----|--------|-------|-----------|------------|

## 2. Graph addition plan (onto Trunk SoT)

Only confirmed dispositions below should be applied to `spec/00–50`. POC binder remains non-SoT.

| ID | disposition | suggested Trunk action |
|----|-------------|------------------------|

> Note: no topic-owned draft clauses to deprecate. Reject path is binder archive + DEC (`Rejects:`) only.

### Add nodes

- _(none — all listed IDs already placed or keep-draft)_

### Add / update edges

- Review existing meta edges; attach new clauses to Trunk parents only.

### Do not add

- POC `evidence/` QPS/Recall numbers as `status=stable` must SLA ([[CON-POC-001]])
- Entire `poc/<topic>/ndf/` tree as a second SoT
- Unconfirmed amend proposals without draft→stable ID list

### Suggested free IDs (if creating new clauses)

- `BEH-036`
- `API-020`
- `CON-SLA-021`
- `VER-045`
- `DEC-099`

## 3. Prose provenance (POC 溯源 — required)

Any prose copied into Trunk (rationale, DEC body, VER notes, promote write-up) **MUST** retain source lines:

```text
> source: poc/cluster-gbdt/ndf/TOPIC.md ; proposals/<file> ; evidence/ ; COMMITS.md @ a9c76de
> track: reject ; Topic: cluster-gbdt
```

Per-artifact templates:

- Evidence dir `poc/cluster-gbdt/ndf/evidence`: cite round + date; do not paste as must SLA.
- TOPIC narrative (`Active hypothesis` / results): `poc/cluster-gbdt/ndf/TOPIC.md`

**Forbidden:** pasting POC prose into `spec/00–50` without `source:` lines.

## 4. Deferred finalization checklist (not executed by this tool)

- [ ] Set `TOPIC.md` status → `rejected`
- [ ] Sync `NOTES.md` header status → `rejected` (or N/A if no NOTES.md) ([[BEH-025]])
- [ ] Write/update product DEC with `Rejects: cluster-gbdt`
- [ ] Move binder → `spec/archive/2026-08/poc-cluster-gbdt/`
- [ ] If any topic-owned drafts existed: leave `deprecated` shells in Trunk
- [ ] `spec/open/` stubs for Implemented proposals if still pointing at live paths

## 4b. Semantic core (MODEL)

Semantic core: **N/A (reject)** — no distill decision required ([[META-004]]).

## 4c / 4d. Baseline & surface

Baseline invalidation / surface overlap: **N/A (reject)** ([[BEH-025]]).

## 5. Post-merge checks before final close (MUST run)

After applying graph additions to Trunk SoT:

```bash
python3 spec/meta/tools/ndf_index.py index
python3 spec/meta/tools/ndf_graphcheck.py --report tmp/graphcheck-after-close-cluster-gbdt.md
```

**Pass criteria for this close:**

- `ndf_graphcheck` reports **no new hard errors** on touched IDs: 
- Pre-existing warehouse cycles (e.g. meta `CHR-008`↔`BEH-018`) may remain; document them as pre-existing — but **this close must not** add `stable/must`→`draft` refine/depends edges.
- Re-run `python3 spec/meta/tools/ndf_index.py poc-topics` to confirm binder status.

---

_End of plan. Mode=reject. No SoT files were modified by `spec/meta/tools/ndf_close.py`._
